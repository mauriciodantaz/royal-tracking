---
description: Use local .env Supabase credentials; do not use MCP Supabase for this project
alwaysApply: true
---

# Supabase — credenciais deste projeto

- **Não use o MCP Supabase** (`plugin-supabase-supabase`) neste repo. Ele está autenticado em outra conta (org `dt` / projeto `orcamentos`) e **não tem permissão** no projeto correto.
- Use só as credenciais do `.env` local:
  - `SUPABASE_URL=https://tdgaitwvakzztcbodwfm.supabase.co`
  - `SUPABASE_ANON_KEY` (anon/public do Dashboard → Settings → API)
- Para schema/SQL/migrations: CLI local (`npx supabase ...`), SQL Editor no dashboard, ou scripts com a anon key (ou `service_role` só em backend, se existir no `.env`).
- Nunca assuma que `list_projects` / `get_project` do MCP apontam para este projeto.
- Não commitar `.env`; o template fica em `.env.example`.
